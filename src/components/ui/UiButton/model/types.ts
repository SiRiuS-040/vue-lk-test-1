export enum ButtonTheme {
    PRIMARY = 'primary',
    OUTLINE = 'outline',
    PLAIN = 'plain',
    CLEAR = 'clear'
}

export enum ButtonFormat {
    DEFAULT = 'default',
    SQUARE = 'square',
    CIRCLE = 'circle'
}

export enum ButtonSize {
    S = 's',
    M = 'm',
    L = 'l'
}